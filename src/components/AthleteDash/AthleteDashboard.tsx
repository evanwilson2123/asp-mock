// import { IAthleteTag } from '@/models/athleteTag';
// import { IGoal } from '@/models/goal';
import React from 'react';

// interface BodyWeightChart {
//   weight: number;
//   date: Date;
// }

const AthleteDashboard = () => {
  // manage the state for swing and pitch counts
  //   const [swingCount, setSwingCount] = useState<number>(0);
  //   const [workoutCount, setWorkoutCount] = useState<number>(0);
  //   const [pitchCount, setPitchCount] = useState<number>(0);
  //   // manage the state for the current goals in progress (maybe toggle completed)
  //   const [goals, setGoals] = useState<IGoal[]>([]);
  //   // this needs an SQL database table
  //   const [bodyWeightData, setBodyWeightData] = useState<BodyWeightChart[]>([]);
  //   // manage the state for the tags assigned to them
  //   const [tags, setTags] = useState<IAthleteTag[]>([]);

  // these are mocks for future implementation
  // const [upcomingWorkouts, setUpcomingWorkouts] = useState<??[]>([]);

  return (
    <div className="flex justify-center">
      <h1 className="text-red-600 text-2xl">Athlete Dash Board</h1>
    </div>
  );
};

export default AthleteDashboard;
