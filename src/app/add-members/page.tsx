import AddMembersPage from '@/components/AddMembers';
import React from 'react';

/*
 * - Page to add members (Coaches and Athletes) to the facility
 * - using the <AddMembersPage /> component
 */
const page = () => {
  return (
    <div>
      <AddMembersPage />
    </div>
  );
};

export default page;
